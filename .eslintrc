{
  "extends": "hypothesis",
  "rules": {
    "prefer-arrow-callback": "error",
    "prefer-const": ["error", { "destructuring": "all" }]
  },
  "overrides": [
    {
      "files": ["*.js"],
      "excludedFiles": ["h/**"],
      "env": { "node": true },
    }
  ]
}
