{
  "extends": "hypothesis",
  "rules": {
    "indent": "off",
    "no-var": "error",
    "prefer-arrow-callback": "error",
    "prefer-const": ["error", { "destructuring": "all" }],
    "space-before-blocks": "error"
  }
}
