# Most code in migrations it's autogenerated
"h.migrations.*",
# We'd want to enable typechecking in all of these
# but to help kickstart typechecking we'll start with a
# long list of ignored modules here that we should eventually reduce
"h.cli.*",
"h.emails.*",
"h.migrations.*",
"h.security.*",
"h.services.bulk_api.*",
"h.streamer.*",
"h.subscribers.*",
"h.traversal.*",
"h.viewderivers.*",
"h.views.*",
