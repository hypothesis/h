Organization:
  type: object
  required:
    - id
    - name
  properties:
    id:
      type: string
    logo:
      type: string
      format: uri
      description: URI to logo image
    name:
      type: string
