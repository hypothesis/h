<div class="form-checkbox">
  <input type="checkbox"
         name="{{ field.name }}"
         id="{{ field.oid }}"
         value="{{ field.widget.true_val }}"
         {%- if cstruct == field.widget.true_val %}
         checked=""
         {% endif -%}>
</div>
