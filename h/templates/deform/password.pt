<input type="password" name="${field.name}" value="${cstruct}"
  tal:attributes="size field.widget.size;
  class string:${field.widget.css_class} form-input | nothing;
  placeholder field.title | nothing"
  id="${field.oid}"/>

