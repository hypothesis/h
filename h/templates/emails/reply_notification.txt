${reply_user} has just left a reply on your annotation in "${document_title}".
${document_path}

On ${parent_timestamp.strftime('%d %B at %H:%M')} you said:

"${parent_text}"

On ${reply_timestamp.strftime('%d %B at %H:%M')} ${reply_user} said:

"${reply_text}"

View this reply: ${reply_path}
or view anna's stream: ${reply_user_profile}
