<input type="password" name="${field.name}" value="${cstruct}"
       tal:attributes="size field.widget.size;
                       class field.widget.css_class;
                       placeholder getattr(field.schema, 'placeholder', '')"
       id="${field.oid}"/>

