'use strict';

describe('rootThread', function () {
  // TODO

  // - Builds a thread using the current list of annotations,
  //   selection, expanded and edited sets from annotationUI

  // - Sorts and filters appropriately

  // - Updates the annotation set when annotations are created,
  //   loaded or deleted

  // - Expands the parents of new annotations

  // - Rebuilds the thread when annotations are created,
  //   loaded or deleted

  // - Rebuilds the thread when sortBy and setSearchQuery()
  //   are called
});
