// Generated by CoffeeScript 1.6.3
/*
** Annotator 1.2.6-dev-accf82f
** https://github.com/okfn/annotator/
**
** Copyright 2012 Aron Carroll, Rufus Pollock, and Nick Stenning.
** Dual licensed under the MIT and GPLv3 licenses.
** https://github.com/okfn/annotator/blob/master/LICENSE
**
** Built at: 2015-01-17 10:04:34Z
*/



/*
//
*/

// Generated by CoffeeScript 1.6.3
(function() {
  var TextPositionAnchor, _ref,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

  TextPositionAnchor = (function(_super) {
    __extends(TextPositionAnchor, _super);

    TextPositionAnchor.Annotator = Annotator;

    function TextPositionAnchor(anchoring, annotation, target, start, end, startPage, endPage, quote, diffHTML, diffCaseOnly) {
      this.start = start;
      this.end = end;
      TextPositionAnchor.__super__.constructor.call(this, anchoring, annotation, target, startPage, endPage, quote, diffHTML, diffCaseOnly);
      if (this.start == null) {
        throw new Error("start is required!");
      }
      if (this.end == null) {
        throw new Error("end is required!");
      }
      this.Annotator = TextPositionAnchor.Annotator;
    }

    TextPositionAnchor.prototype._getSegment = function(page) {
      var browserRange, mappings, normedRange, realRange;
      mappings = this.anchoring.document.getMappingsForCharRange(this.start, this.end, [page]);
      realRange = mappings.sections[page].realRange;
      browserRange = new this.Annotator.Range.BrowserRange(realRange);
      normedRange = browserRange.normalize(this.anchoring.annotator.wrapper[0]);
      return {
        type: "magic range",
        data: normedRange
      };
    };

    return TextPositionAnchor;

  })(Annotator.Anchor);

  Annotator.Plugin.TextPosition = (function(_super) {
    __extends(TextPosition, _super);

    function TextPosition() {
      this.createFromPositionSelector = __bind(this.createFromPositionSelector, this);
      this._getTextPositionSelector = __bind(this._getTextPositionSelector, this);
      _ref = TextPosition.__super__.constructor.apply(this, arguments);
      return _ref;
    }

    TextPosition.prototype.pluginInit = function() {
      this.Annotator = Annotator;
      this.anchoring = this.annotator.anchoring;
      this.anchoring.selectorCreators.push({
        name: "TextPositionSelector",
        describe: this._getTextPositionSelector
      });
      this.anchoring.strategies.push({
        name: "position",
        code: this.createFromPositionSelector
      });
      return this.Annotator.TextPositionAnchor = TextPositionAnchor;
    };

    TextPosition.prototype._getTextPositionSelector = function(selection) {
      var document, endOffset, startOffset;
      if (selection.type !== "text range") {
        return [];
      }
      document = this.anchoring.document;
      if (document.getStartPosForNode == null) {
        return [];
      }
      startOffset = document.getStartPosForNode(selection.range.start);
      endOffset = document.getEndPosForNode(selection.range.end);
      if ((startOffset != null) && (endOffset != null)) {
        return [
          {
            type: "TextPositionSelector",
            start: startOffset,
            end: endOffset
          }
        ];
      } else {
        if (startOffset == null) {
          console.log("Warning: can't generate TextPosition selector, because", selection.range.start, "does not have a valid start position.");
        }
        if (endOffset == null) {
          console.log("Warning: can't generate TextPosition selector, because", selection.range.end, "does not have a valid end position.");
        }
        return [];
      }
    };

    TextPosition.prototype.createFromPositionSelector = function(annotation, target) {
      var content, corpus, currentQuote, document, savedQuote, selector, _base;
      selector = this.anchoring.findSelector(target.selector, "TextPositionSelector");
      if (selector == null) {
        return;
      }
      if (selector.start == null) {
        console.log("Warning: 'start' field is missing from TextPositionSelector. Skipping.");
        return null;
      }
      if (selector.end == null) {
        console.log("Warning: 'end' field is missing from TextPositionSelector. Skipping.");
        return null;
      }
      document = this.anchoring.document;
      corpus = typeof document.getCorpus === "function" ? document.getCorpus() : void 0;
      if (!corpus) {
        return null;
      }
      content = corpus.slice(selector.start, selector.end).trim();
      currentQuote = this.anchoring.normalizeString(content);
      savedQuote = typeof (_base = this.anchoring).getQuoteForTarget === "function" ? _base.getQuoteForTarget(target) : void 0;
      if ((savedQuote != null) && currentQuote !== savedQuote) {
        return null;
      }
      return new TextPositionAnchor(this.anchoring, annotation, target, selector.start, selector.end, document.getPageIndexForPos(selector.start), document.getPageIndexForPos(selector.end), currentQuote);
    };

    return TextPosition;

  })(Annotator.Plugin);

}).call(this);

//
//# sourceMappingURL=annotator.textposition.map