# Matcher object for PDF.js documents
#
# All the required features are present in the ancestor class
# and the PDF Text Mapper class, so we don't need anything special here.
class window.PDFTextMatcher extends DTM_MatcherCore

  constructor: (mapper) ->
    super
